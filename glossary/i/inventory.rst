.. inventory:

Inventory
---------

The process of collecting specific node data; for example, hardware
specification, rack location in the data-center, and so on.

Fuel nailgun uses the collected data to calculate the values of
serialized attributes; for example, partitioning schema.