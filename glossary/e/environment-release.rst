.. environment-release:

.. index:: environment, release, Fuel, MOS, MOM, MCP

Environment release
-------------------

A preset of parameters applied to the cluster from the template ``Release``.
The template ``Release`` specifies a set of common attributes such as
repository list, default list of roles, default node partitioning, networks
allocation, and so on.